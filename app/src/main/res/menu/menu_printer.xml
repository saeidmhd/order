<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/prntDialog"
        android:name="@string/find_printer"
        android:title="@string/find_printer"></item>
    <item
        android:id="@+id/disconnect"
        android:enabled="false"
        android:name="@string/disconnet"
        android:title="@string/disconnet"></item>

</menu>